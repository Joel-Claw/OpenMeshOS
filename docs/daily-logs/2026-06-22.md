# OpenMeshOS Daily Log — 2026-06-22

## 5 AM Security Check

### GitHub Security Review
- **Issues**: 1 (closed, #1 T-Display P4 feature request). No new issues. No malicious content.
- **PRs**: 0 open, 0 closed. Nothing to review.
- **CI status**: All green
  - Build matrix (t-deck + heltec-v3): PASS on dev
  - CodeQL Security Scan: PASS on dev
  - Stale Issues bot: running daily
- **No suspicious activity** detected on the repository.

### Repository Activity
- Last push event: our dev push (June 21)
- No external contributions since stevefoxuk issue #1 (April 24)

## Development Work

### Commits Made
1. `1a7d696` — fix: use build_src_filter (deprecated src_filter alias removed in PIO 6)
   - Replace deprecated `src_filter` with `build_src_filter` in platformio.ini
   - Remove redundant `lib_dir = lib` (lib/ is the default)
   - Applies to t-deck and heltec-v3 environments

2. `e0a9116` — fix: OpenMeshChat getContactByIdx returns correct contact by index
   - **Bug**: `getContactByIdx` was always returning the first contact instead of
     the one at the requested index. The `ContactsIterator` was created but `hasNext()`
     was only called once, never advancing to the desired index.
   - **Fix**: Loop through the iterator `idx+1` times to reach the correct contact.
   - Added file header (magic `0x4F4D5343` + version + count) to contacts persistence
     format for forward compatibility and old-format detection.
   - `loadContacts` now uses saved count instead of reading until EOF.

3. `3a2bf81` — docs: update TODO for OpenMeshChat fixes

### Build Status
- `t-deck`: PASS (Flash: 25.0%, RAM: 30.3%)
- `heltec-v3`: PASS (Flash: 17.9%, RAM: 18.1%)
- Host tests: 7/7 passed, 2 skipped (Arduino filesystem tests)

### Files Modified
- `platformio.ini` — build_src_filter migration
- `src/mesh/OpenMeshChat.cpp` — getContactByIdx fix + contacts persistence header
- `docs/TODO.md` — updated OpenMeshChat section

### Next Steps
- OpenMeshChat integration into MeshService (major refactor, needs careful planning)
- Hardware testing on T-Deck (flash + verify)
- Review LilyGo official T-Deck examples (still pending in TODO)
- RAK WisBlock platform support (Phase 4)